table:
  name: org_usage_current_month
  schema: public
  select_permissions:
    - role: viewer
      permission:
        columns: '*'
        filter:
          org_id:
            _in:
              - X-Hasura-User-Id  # placeholder – will be constrained via org_members join
    - role: editor
      permission:
        columns: '*'
        filter:
          org_id:
            _in:
              - X-Hasura-User-Id
    - role: owner
      permission:
        columns: '*'
        filter:
          org_id:
            _in:
              - X-Hasura-User-Id
  insert_permissions: []
  update_permissions: []
  delete_permissions: []
