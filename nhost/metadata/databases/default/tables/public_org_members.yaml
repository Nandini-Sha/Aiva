table:
  name: org_members
  schema: public
object_relationships:
  - name: organization
    using:
      foreign_key_constraint_on: org_id
select_permissions:
  - role: user
    permission:
      columns:
        - id
        - org_id
        - user_id
        - role
        - created_at
      filter:
        organization:
          org_members:
            user_id:
              _eq: X-Hasura-User-Id
insert_permissions:
  - role: user
    permission:
      check:
        organization:
          org_members:
            role:
              _eq: owner
            user_id:
              _eq: X-Hasura-User-Id
      columns:
        - org_id
        - user_id
        - role
update_permissions:
  - role: user
    permission:
      columns:
        - role
      filter:
        organization:
          org_members:
            role:
              _eq: owner
            user_id:
              _eq: X-Hasura-User-Id
      check: null
delete_permissions:
  - role: user
    permission:
      filter:
        organization:
          org_members:
            role:
              _eq: owner
            user_id:
              _eq: X-Hasura-User-Id
