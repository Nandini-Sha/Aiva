type Mutation {
  triggerWorkflowRun(
    workflow_id: uuid!
  ): ActionResponse

  approveStep(
    step_run_id: uuid!
  ): ActionResponse
}

type ActionResponse {
  success: Boolean!
  message: String
  run_id: uuid
}
