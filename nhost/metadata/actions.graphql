type Mutation {
  triggerWorkflowRun(
    workflow_id: uuid!
  ): ActionResponse
}

type Mutation {
  approveStep(
    step_run_id: uuid!
  ): ActionResponse
}

type ActionResponse {
  success: Boolean!
  message: String
  run_id: uuid
}
